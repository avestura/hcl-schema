{
  "name": "hcl-schema",
  "publisher": "avestura",
  "displayName": "HCL Schema",
  "description": "Support for .schema.hcl files",
  "icon": "assets/hashicorp_logo_mark_light_universal_radius45.png",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.106.1"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "HashiCorp",
    "devops",
    "hcl",
    "schema"
  ],
  "bugs": {
    "url": "https://github.com/avestura/hcl-schema/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/avestura/hcl-schema.git"
  },
  "homepage": "https://github.com/avestura/hcl-schema",
  "galleryBanner": {
    "color": "#FFF",
    "theme": "light"
  },
  "extensionDependencies": [
    "hashicorp.hcl"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onLanguage:hcl"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "hcl-schema.validateActive",
        "title": "Validate active HCL file"
      }
    ]
    ,
    "configuration": {
      "type": "object",
      "title": "HCL Schema",
      "properties": {
        "hclSchema.cliPath": {
          "type": "string",
          "default": "",
          "description": "Path to the hclschema CLI binary. If empty the extension will use `go run ./cmd/hclschema-cli` in development."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "yarn run build:go-cli && yarn run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "build:go-cli": "node ./scripts/build-go-cli.js",
    "pretest": "yarn run compile && yarn run lint",
    "lint": "eslint src",
    "test": "vscode-test",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/vscode": "^1.106.1",
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "typescript-eslint": "^8.46.3",
    "eslint": "^9.39.1",
    "typescript": "^5.9.3",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2"
  }
}
